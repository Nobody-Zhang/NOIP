#include<iostream>
#include<cstdio>
#include<cstring>
#include<algorithm>
#include<ctime>
using namespace std;

long long rd(long long l,long long r){
	return (long long)rand()*rand()*rand()%(r-l+1) ;
}

int main(){
	srand(time(NULL)+(long long)new int) ;
	long long M=1000000000000000000 ;
	long long L=rd(1,1000) , R=rd(10000,100000) ;
	cout << L << ' ' << R << '\n' ;
	return 0 ;
}
