#include<bits/stdc++.h>
using namespace std;
string s,w,v[1005];
char a[105];
int main()
{
	int p,b,k;
	getline(cin,s);
	p=s.size();
	for(int i=1;i<=p;i++)
	{
		if(s)
	 v[i]=s[i];
	}
	/**/
	getline(cin,w);
	for(int i=1;i<=a;i++)
	  if(v[i]==b)
	   {
	   	   a[i]
	   }
	
	cout<<a;
}
